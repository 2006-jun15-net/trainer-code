{
  "name": "video-watcher",
  "version": "1.0.0",
  "description": "video watcher",
  "repository": {
    "type": "git",
    "url": "https://github.com/2006-jun15-net/trainer-code.git"
  },
  "main": "index.js",
  "scripts": {
    "build": "rm -rf out dist && tsc && webpack && cp src/*.html dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nick Escalona",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "typescript": "^3.9.7",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  }
}
